# IFTA_team5
letsgoooooooooo

1번 git clone을 한다
2번 현호가 올려놓은 json 파일을 bigquery 폴더에 넣는다
3번 ADS-B-Group.groupproj를 build all을 한다
4번 ADS-B-Display.cbproj 이거 build한다
5번 terminal/powershell 열고 pybackend로 cd
6번 $env:GEMINI_API_KEY="여기에_네_API_KEY"
7번 uvicorn main:app --port 8001
8번 서버 켜졌으면 run